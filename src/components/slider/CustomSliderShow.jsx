import { useEffect } from "react";

export default function CustomSliderShow({ children }) {
    const rooms = children;
    /*useEffect(() => {
        console.log("export default function CustomSliderShow() => rooms:", rooms)
    }, [children]);*/
}