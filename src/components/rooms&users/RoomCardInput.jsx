export default function RoomCardInput({children}) {
    //customInput={{name, function},{name, function},{name, function}}
    //const placeholder = props.placeholder;
    //const customFunction = props.customFunction;
    //const numberOfInputs = customInput.length + 1;
    return <div>
        {children}
    </div>
}